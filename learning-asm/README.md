# learning-asm

> [!NOTE]
> I do want to stress that the C code functions were generated by AI (**except** `bubble_sort.c`, `factorial.c`, `fib.c`, `max.c`, and `strlen.c`, which I made myself at some point and grabbed from the places I made them), as I used AI to 'assist' in my learning of assembly. By assist, I asked it for a function where I could try doing `x` thing, and asked for suggestions of what I should learn and for clarifications.
>
> The assembly code (`.asm` files) and their comments (`;`) were written entirely by me with AI only used for feedback (which I took on and tried to modify my assembly accordingly).

I attempt to learn assembly 🙏

Each C source file is a reference for me to translate into assembly, as that's easier mentally for me to convert.

At the top of each file lies a comment detailing the date and time I created that file, making it easier to see my progression through the language. I never went back and modified after the timestamp specified at the top of the file. (quite a few are missing this, and that's because I got lazy. So some new ones I make might also be missing some but eventually they all might).